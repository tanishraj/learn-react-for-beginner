const React = require("react");

const ES5Component = function () {
  return (
    <div>
      <h1>Functional Component (ES5 Way):</h1>
      <p>
        Hey there, we can write our functional component in pure ES5 also. You
        wanna see how it looks, check out this component code.
      </p>
    </div>
  );
};

module.exports = ES5Component;
